# -*- coding: utf-8 -*-
# vim: ft=yaml
---
openldap:
  client_config: /etc/ldap.conf
  client_pkg: openldap
  server_defaults: /etc/default/slapd
  server_pkg: openldap
  service: slapd
  user: openldap
  group: openldap
  database: mdb
  su_group: root
  index: objectClass
